configurations {
    shadow {
        attributes {
            attribute(Attribute.of("rfgDeobfuscatorTransformed", Boolean.class), Boolean.TRUE)
        }
    }
}

dependencies {
    compileOnly fileTree(project.file("libraries"))
    compileOnly "org.jetbrains:annotations:24.0.1"

    testImplementation platform("org.junit:junit-bom:5.10.0")
    testImplementation "org.junit.jupiter:junit-jupiter"
}

tasks.test {
    useJUnitPlatform()
}